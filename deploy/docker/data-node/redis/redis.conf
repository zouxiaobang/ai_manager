# Docker 数据节点 Redis：允许局域网访问（与 backend.env 中 REDIS_HOST=192.168.0.116 配合）
bind 0.0.0.0
port 6379
protected-mode no

# 持久化（可选，默认 RDB）
save 900 1
save 300 10
save 60 10000

dir /data

# 若需密码，取消下行注释并在 backend.env 增加 REDIS_PASSWORD（需 Spring 配置支持）
# requirepass 你的Redis密码
